# AIGentOrchestrator / multi-agent orchestration config

# Stage 5 only builds plans. Real downstream execution must remain disabled.
AIGENT_ORCHESTRATOR_ALLOW_EXECUTION=false
AIGENT_ORCHESTRATOR_DEFAULT_MODE=dry-run
