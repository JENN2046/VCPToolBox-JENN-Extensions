# JennImageProviderAdapter

Reviewed persistent AI Image provider-adapter skeleton for the M32 package gate.

Scope:

- provider-adapter package structure
- redacted provider binding metadata
- no-provider dry-run fixture
- default disabled manifest
- no provider calls
- no real image generation
- no runtime adapter discovery

This package is source/package evidence only. It is not active until a later reviewed local gate explicitly enables discovery and registration.
